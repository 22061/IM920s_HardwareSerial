char buf[256];
int i = 0;

void setup() {
  Serial.begin(115200);
}

void loop() {
  if(Serial.available()) 
  {
    buf[i] = Serial.read();
    if(buf[i] == '\n') 
    {
      i = 0;
    }
    else
    {
        i++;
    }
  }
}
